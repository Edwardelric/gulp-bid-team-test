require(['jquery','bootstrap-sass','common'],function($){
    //保证金 管理列表
    $(function(){

        // var $btnSearch = $('.search-form .btn-search'),
        //     $tenderTitle = $('.search-form [name=tenderTitle]'),
        //     $status = $('.search-form [name=status]');
        //
        //
        // // // 提交查询
        // // $btnSearch.click(function() {
        // //     var $tag = $(this),
        // //         hrefStr = '/deposit/depositList?type=' + $tag.attr('data-type'),
        // //         paramStr = '';
        // //
        // //     if($.trim($tenderTitle.val())) {
        // //         paramStr = '&tenderTitle=' + $tenderTitle.val();
        // //     }
        // //     if($.trim($tenderTitle.val())) {
        // //         paramStr = paramStr + '&status=' + $status.val();
        // //     }
        // //     if(paramStr) {
        // //         hrefStr = hrefStr + paramStr;
        // //     }
        // //
        // //     $tag.attr('href', hrefStr);
        // // });
        //
        //
        // $tenderTitle.change(function() {
        //     this.value = $.trim(this.value);
        // });


    });
});

